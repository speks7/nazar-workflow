Tf-Hub is new way of using Inception
Just:
    ```
    sudo pip install tensorflow_hub
    sudo pip install --upgrade tensorflow
    ```

Add replace the files in Class-Model with same usage basis

When training the model, add: --tfhub_module https://tfhub.dev/google/imagenet/mobilenet_v2_140_224/feature_vector/1 for MobileNet V2