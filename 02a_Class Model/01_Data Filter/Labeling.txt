1 Label the images by bulk renaming images inside the folder(inception is capable just by creaating with labelled folder though) with the respective folder name or use labelimg

2 Create a single direcort having all the images like
    images /
        - capacitor / cap01.jpg , ... , cap50.jpg
        - inductor / ind01.jpg , ... , ind50.jpg

3 Create a directory as:
    class-label/
        - images
        - 

4 Open the newly duplicated directory and rename .jpg to .jpg.txt
    ```
    $ rename .jpg .jpg.txt *.jpg
    $ truncate -s 0 *.jpg.txt
    $ python3 filename.py /path/to/duplicate_dir
    ```
    which will rename the .jpg to .jpg.txt and then clear all line inside the .txt with addition on the label defined by the filename